# Feature Engineering Pipeline
This project creates feature tables from raw data and stores features as partitioned parquet files.
Includes:
- feature_engineer.py producing features.parquet
- metadata.json describing features
- example notebook (markdown) showing usage
