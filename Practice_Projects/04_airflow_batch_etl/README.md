# Airflow Batch ETL (Docker Compose)
This project provides:
- docker-compose to run Airflow locally
- a DAG that triggers an API extraction, runs a PySpark transform, and loads to Postgres
- example DAG file and scripts
